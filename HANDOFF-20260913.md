# JSA checkpoint — vc28 installed and ordinary handoff verified

Repository tester3x/wellbuilt-jsa; branch fix/jsa-app-access-handoff-20260913. APK source f345a627bf3f1bd85a2af0b2c26c5a1d83a72265. EAS 0732ceab-76dc-47dd-ab78-e99947f619b7; versionCode 28; SHA-256 247E92825776B04F996DC59A5CB9F4E431C317FD287D29351592A3ED01304489. Installed on ZFold and S24 over existing data; version 28 verified on both.

## Fixed and verified

The prior ordinary Suite card incorrectly entered required-request recovery with no request. Local PKCE app_access intent now routes through shared callback ownership and full identity verification. Required requests retain their governed path. Every Suite card entry performs fresh Suite authorization; a cached JSA identity alone cannot establish who is currently signed into Suite.

vc27 exposed a second defect: S24 installed the session successfully but its AuthContext presentation stayed stale until restart. vc28 subscribes AuthContext to governed-ready revisions, clears old presentation while rechecking, and rejects stale async reads using read-generation and identity-epoch guards. Loaded identity must match the inspected UID/driver/company. Job Details refresh ownership/dependencies include session generation so same-driver re-login also refreshes.

Device proof on vc28: S24 entered ordinary JSA and correctly displayed off-shift status; returning to Suite and entering again succeeded without restarting. ZFold entered ordinary JSA and correctly displayed No Active JSA Request while its actual shift was open. Settings on each showed its own expected account, not the other phone's profile. No inspection/JSA was signed, job accepted, or shift changed by these checks. Callback durations observed: S24 first 4,592 ms; ZFold 1,275 ms. These are callback measurements, not complete card-to-screen timings or a claimed speed improvement.

## Automated validation

36 of 38 tool test scripts pass. Six new tests exercise the real AuthContext provider with controlled backend promises: revision-driven sign-in, same-driver generation refresh, out-of-order account switch, epoch invalidation, binding mismatch, and unmount. Diagnostic AuthContext hash snapshot updated for the reviewed change. The two legacy source-shape tests logoutClearing and wbtReadRequest still fail as previously reproduced on unchanged a9ae301. Android export passes. TypeScript retains baseline errors in signoff docId, AppSwitcher implicit type, SignaturePad forceDarkAllowed, and PDF EncodingType; no new changed-code errors.

## Separately restored backend lookup

The deployed jsaGetReadRequest omitted wellName required by pending full-read clients. Verified all 235 live src files match base 871ef448. Reused earlier request-bound invoice lookup in isolated C:/dev/jsa-request-well-restore, branch fix/jsa-request-well-restore-20260913. Target-only deployment from b38e14e7 is ACTIVE at jsagetreadrequest-00004-nob. Source and compiled changed modules match; peer revisions unchanged. 138 request checks, 14 SSO spine checks, and 15 emulator direct-database denials pass. Rules and production records untouched. Backend handoff: docs/JSA-WELL-RESTORE-20260913.md.

## Next work

Full read/sign/return testing is still pending. Mike selected Liquid Gold. S24 has queued GABRIEL 2-36-25H and THOR 1-31-30H; neither was accepted or modified. A question is pending about the test job or Mike launching his next real JSA. Do not fabricate or sign an inspection for him.

Suite's separate shift-close and lingering-JSA recovery contract is still unfinished. Read SHIFT-CLOSE-FOLLOWUP-20260913.md; ordinary app access and request display are not proof of that workflow. Keep the dirty C:/dev/wellbuilt-jsa and C:/dev/wb-dvir-completion-firebase checkouts untouched. Commit/push WIP before pauses rather than waiting for completion.

## UI continuation — device verification pending

Added More menu (Job Details, Saved JSAs, Settings, Switcher), removed JSA's floating launcher, reused existing registry/launch behavior in a Switcher list. No identity or governing rule changes. Fixed Android Saved JSAs icon mapping and bottom-tab safe-area spacing. Location/activity pairs remain left/right and single-line with ellipsis for overflow; updated the older test that explicitly forbade this user-requested layout.

36/38 tool suites pass after the requested UI expectation change. Remaining logoutClearing/wbtReadRequest failures are documented baseline failures. Android export passes; TypeScript retains the same four baseline locations. Source changes not yet installed. vc28 remains the verified installed build until the next APK is verified.

Still pending: visual/device check, differing entry-path legacy-screen investigation, authoritative shift-close recovery, actual Liquid Gold JSA test. Liquid Gold JSA mode remains OFF; no JSA signed or records changed. WB-E thermal change is isolated in its own repo. ETC drive timer remains deferred behind WB priorities; Rascal involvement is unproven.

Build submitted: JSA vc29, EAS 40e73390-26fb-496b-aa80-d53dd11bb7c5, exact source 109b806e4ca90795a15e41e97182c05549beec1f. Status IN_PROGRESS at last check; no install claimed. Synthetic summary render checked at 360/412/768 CSS pixels, single-line pairs and no horizontal overflow. Local status JSON: C:/dev/output/dashboard-audit-20260912/jsa29-build-status.json. Download only when FINISHED, verify source/version and hash, install -r on both phones, then inspect menu/tab/card and ordinary entry paths. Do not sign a JSA for Mike.

## Installed vc29 and navigation continuation

vc29 was installed with adb -r on both RFGL23VJCED and R5CX15HEGQB; dumpsys confirmed versionCode 29. APK SHA-256: 1602E16AC49E604C3F05BEEE385D85958C4DD1FA7F3CC5C839EEDB62DE9E1F4B. S24 authenticated app access and menu checked. ZFold reports legacy history verification blocked with jsaStatus 403; this remains unresolved, not an empty-history success. No JSA signed.

Next source adds the requested Saved JSAs | Job Details | More bottom bar. More now opens the WB-E-style two-by-two Switcher modal with JSA excluded and existing registry/tier/launch handling preserved. Main tab header duplicates removed; downstream header menus retained. No authentication, rules, or production records changed. This navigation continuation is not yet built or installed. TypeScript retains the same four baseline error locations.
Android export for this navigation checkpoint passed; git diff --check passed. Device interaction verification remains pending.

## Standalone JSA authorization continuation (WIP; not enabled)

User requirement: every user belongs to a company, including one-truck owner-operators. Independent JSA creation requires authenticated company permission, not an active shift. Only an explicit required launch binds work to a verified request/shift. Added a pure creation-access contract and 13 boundary tests; not yet wired into production UI because save/read/close must be secured together.

Confirmed blockers in existing client: index.tsx hides standalone form whenever session.authKind is governed (all modern logins); resolveMode derives required workflow from any verified cached shift; signoff.tsx uses cached shift or a date as shiftId and invokes legacy unauthenticated REST writes; sync.ts uploads all device records through direct SDK writes. These must not be re-enabled as the standalone implementation. ZFold history guard failure remains unresolved, not proof of no older JSAs.

Verified backend checkout C:/dev/jsa-request-well-restore at a4fb4933 has submitJsaRecord (security/operational/jsaWrite.ts). It accepts legacy hash fallback, generic payload and optional dayStatus, and lacks an explicit company JSA entitlement check. It is not a sufficient standalone contract without isolated review/correction. This checkout does not contain jsaPersistGovernedArtifact despite the client referencing it. Reconcile exact live function source before any deployment; no broad/rules deploy.

Next: establish authenticated company-authorized standalone persist/list/read/close using canonical live lineage, preserve immutable owner-bound records/idempotency, no shift/day-status/required receipt for independent work. Then wire access policy, owner-scoped local history/sync and signoff, test denial and required-handoff isolation, build and install both phones. No new APK submitted for this incomplete continuation. Existing installed vc29 and pushed navigation 3fbc30d unchanged.

## Standalone client candidate

Company-authorized standalone access now controls the ordinary form; cached shift alone no longer selects required workflow. Signoff requires live session plus server access, stores workflow=standalone and shiftId=null, uses jsaStandalone, and never invokes the legacy REST/day-status/receipt path for independent work. Retry uses the form session's local identity and retains its first saved payload. Owner-scoped Saved JSAs sync uses own-record listing; standalone Close preserves signed content. Existing direct sync excludes standalone records. Required launches retain existing isolation and artifact completion flow.

Backend isolated branch feat/jsa-standalone-company-access-20260913 source 8b7dbc87 deployed only functions:dashboard:jsaStandalone. ACTIVE jsastandalone-00001-laj at 2026-09-13T19:37:51.144382466Z. All nine previously inventoried JSA revisions unchanged. No rules or production records written. Exact live artifact source matched 237 files except new index export. Build passed; 23 Firestore-emulator checks, including direct-read denial. Handler identity tests are injected, not real phone token proof.

Client: 37/39 mjs suites pass (logoutClearing/wbtReadRequest remain baseline); updated two Suite-card expectations that explicitly required hiding standalone creation, per new user requirement. Seven client transport/identity boundary checks pass. Android export passes; same four TypeScript baseline locations. On-device real callable access, signature save, read/close, and ZFold older-JSA recovery still require verification. No signed record manufactured. S24 currently connected; ZFold disconnected. New bottom bar/Switcher are included.
vc30 build 262c32e8 was canceled before installation. Final entry correction restricts standalone date matches to the authenticated owner and keeps standalone entry on Job Details rather than auto-opening a shift/history record. Superseding APK will include this source.

## vc31 installed; active-shift ordinary-entry correction

vc31 EAS dffd5ca3-e117-45e0-8f0c-7fcb7a88a8be from b0dcbd64b2501d5d5d7795294f690feb0f469e50 finished and installed with adb -r on both phones. SHA256 0AA5A2492EEBD491286AF1204B9B956900ABF69DA3C9F0D90DC43AF0C4E3C1CD. Both report versionCode 31. S24 opens the company-authorized standalone form, Saved JSAs, bottom More menu and four-other-app Switcher. No JSA signed. ZFold ordinary Suite entry still showed the historical verification gate.

Root cause found in actual index.tsx resolveMode: it STILL selected required mode from verified cached shift alone. Earlier handoff language implying that check was already corrected was inaccurate. The standalone form/access and history corrections were present, but this callback was missed. Current patch requires a launch as well as the verified shift, and refreshes on session generation. Company entitlement and required-request isolation gates are retained.

Five actual callback cases plus renderer gate checks pass (test-jsaEntryMode.cjs), Suite card 17/17 and standalone client 7 checks pass. Android export passes; same four TypeScript baseline locations. New APK/device verification pending. Liquid Gold automatic JSA mode stays Off. Required read/sign/return and shift-close history remain unfinished; do not claim full workflow verification from ordinary entry.

vc32 submitted: EAS b34d2a25-5f96-49dd-b89a-3ab7daa2f928, exact source 2046d69d9dd43c6b7952cdb826b1aa8e7faa2c3c, versionCode32. Broader suite remains 37/39 with WBS_REPO_ROOT=C:/dev/wbs-config-handoff-fix; callback owner 24/24 (default sibling Suite path does not exist on this laptop). Only logoutClearing/wbtReadRequest baseline failures remain.

Local helper C:/dev/output/dashboard-audit-20260912/finish-jsa32.ps1 waits at most 40 minutes, validates FINISHED/source/version, downloads/hashes, and installs -r only on connected S24/ZFold. It does not launch screens, sign records, or clear data. Check jsa32-install-result.json before claiming installation; until that result exists installed version remains vc31. Device UI verification after installation is still required, especially ZFold ordinary Suite-card entry with active shift.
