# JSA ordinary Suite access repair

## vc28 source continuation

AuthContext now subscribes to the existing governed-ready revision signal (which also emits on subscription). It clears old presentation while rechecking the full startup identity, then binds the loaded session to the inspected UID/driver/company. Read-generation and identity-epoch guards suppress stale completions after a newer sign-in, logout, or unmount. Job Details refresh ownership and dependencies include UID and session generation, including same-driver replacement sessions.

Six tests exercise the real provider with mocked React hooks/backend promises: initial sign-in revision, same-driver new generation, out-of-order account switch, epoch invalidation, inspected/loaded binding mismatch, and unmount. All pass. 36 of 38 test scripts pass; only the two previously reproduced baseline failures remain. The diagnostic test's AuthContext snapshot was updated for this reviewed change; diagnostic transport itself is unchanged. Android export passes. TypeScript retains the four documented baseline locations; no new changed-code errors.

Build/install/device test pending. User selected Liquid Gold for continued testing. Do not sign on the user's behalf or manufacture a job for a test.

Base: a9ae301689772f12eadff46a4a3982c232389dfc (existing recovery work). Isolated continuation: fix/jsa-app-access-handoff-20260913. The other dirty wellbuilt-jsa checkout is untouched.

Reproduced on ZFold vc24: ordinary off-shift Suite card entry successfully installed the authenticated JSA session, then incorrectly called required-request recovery with no request, showing "This JSA request is not valid" and Return to Tickets.

The locally minted PKCE attempt now records app_access intent for Suite-card/direct-icon authorization. After successful session installation, shared callback routing opens ordinary JSA only when that intent exists, the session is usable, and no governed request is active. Required-request callbacks still use the original recovery path. No intent supplied in a URL grants access or completion. Saved JSA records are retained. Callback timing logs contain duration and result only.

Suite-card entry always performs fresh Suite authorization, even if JSA's cached identity is internally valid. An old JSA session alone cannot prove who is currently signed into Suite on a shared phone. Full startup verification remains required before ordinary callback navigation. Direct icon access to a valid owned session remains unchanged. Intermediate builds 25 and 26 were canceled before installation as this review tightened the entry contract.

Validation: 35 of 37 tool test scripts pass, including 34 new app-access cases and 24 callback-owner checks. Two legacy scripts (logoutClearing and wbtReadRequest) fail identically on the unchanged a9ae301 baseline; their source-shape expectations predate the existing recovery changes. Callback-owner tests now accept WBS_REPO_ROOT instead of requiring a checkout literally named Suite. Android export passes. Full typecheck has existing errors in signoff.tsx (out-of-scope docId), AppSwitcher, SignaturePad.forceDarkAllowed, and jsaPdf.EncodingType; no new callback errors.

Both phones now have baseline JSA vc24 (EAS 225bbca4-921e-4585-a4c1-71c912361dda), installed over existing data on S24 to match ZFold. New fix is not yet device-verified. Do not claim required-request signing/closeout complete from ordinary app-entry tests.

## vc27 device checkpoint (supersedes the installation status above)

Source d6823335aa2cf5b4d4a8c5d617d55822d8456a11; EAS 39e97537-2bb4-4897-933e-7400f077e2be. APK SHA-256 A626F61A4B9AE91DA0D0BF56D79989A6E6547CFF6F1B5AC310073C8F1F86B56A. Installed over existing data on both phones; versionCode 27 verified on both.

ZFold Suite card reached ordinary off-shift JSA and Saved JSAs without the old invalid-request error. S24 exchange/install succeeded and Saved JSAs opened, but Job Details remained on Signing in until JSA was restarted. After restart it correctly showed securely signed in / Active Shift Required. This is NOT a complete two-device handoff pass.

Likely next defect: AuthContext checks presentation identity only on mount, while the root subscribes to governed session revisions. Job Details compares fresh persisted identity with context session; a stale context can leave refresh superseded. Review and fix revision-driven context hydration with ownership/generation guards, including same-driver session changes; do not bypass checks or treat restarting as the fix. This remains a hypothesis supported by code and the restart result, not an implemented correction.

Callback timings (not full card-to-screen): ZFold 4,333 ms, S24 1,271 ms. No JSA signed or completed. Required-request and Suite shift-close tests remain pending, as described in SHIFT-CLOSE-FOLLOWUP-20260913.md.

Next: install the new APK on both, verify off-shift Suite entry and History without a fabricated request, then required-request ownership/completion and startup replay behavior. Measure handoff stages before changing security checks. WB-E vc34 compact printing is physically confirmed; its separate vc35 signature/continuous-print build is underway from 1fbf1f59dd8af9c2335021bcfc4740702c90dd48.
