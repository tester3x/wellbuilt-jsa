# JSA three-item verification checkpoint — 2026-09-14

Branch: fix/jsa-app-access-handoff-20260913.

Ordinary Suite app access now resumes an allowlisted screen scoped to the authenticated user and company. Required JSA launches take priority. Credentials, request IDs and signature parameters are excluded from the saved route. Token refresh does not repeat the standalone Welcome within the running app.

Includes previously committed Open JSAs bottom bar and starting-point duplicate guard. Add Location selected-record isolation and thermal permission/setup feedback are retained from vc44.

Validation: TypeScript, screen-resume, Welcome/request routing, standalone client, open-record ownership, document generation and thermal transport fixtures pass. Thermal fixtures cover two paired printers, changing selection, removal of the selected device, no send to missing devices, all-page Brother job and ESC/POS page traversal.

Physical verification still required: ordinary WB S return, manually acknowledged Add Location on an open JSA, and complete readable thermal output. Do not treat fixture results as those device results. No live signature, acknowledgement or close was generated by automation.

Next: build preview APK from this checkpoint, install on connected devices without interrupting an active JSA form, and finish these device checks before company-template/Dashboard changes.

## vc45 installation verified

EAS build: 5b15aac5-5767-426f-ae28-975cc61ec533.
Source: 73a20f9f679e221cf07aa56dfc6d3d5b3b75f5f9.
APK SHA-256: 054866F83273E5C692F3218751D3DDCAD3875D9DBB2B0A7EEE95F554FA2307D6.
S24 and ZFold: install succeeded and package versionCode 45 read back. ZFold installation waited until Mike closed JSA. Physical printer and user acknowledgement tests remain pending.

## vc45 switching retest — not passed

Mike paired home printer 4387 while leaving truck printer 7978 paired. After reporting that the selection switched, the next screenshot again displayed RJ-4230B_7978 and OpenStreamFailure. No pages were reported sent. The exact screen on which 4387 appeared is still being clarified; the screenshot alone does not prove an AsyncStorage overwrite.

Follow-up code: settings changes now merge only the changed fields into the latest saved selection. Printing stops without sending if the saved selection differs from the modal's displayed selection. Fixture verifies a width change preserves the latest selected device. TypeScript and thermal fixtures pass. This follow-up is not yet built or device-verified.

## Signature visibility follow-up

The shared read/print renderer omitted the black-stroke filter present in the existing PDF renderer and signoff preview. Added the same brightness(0) rendering for archived signature images, preserving the original stored bytes. Missing/unsupported images now display an explicit unavailable message instead of a silently empty signature section. This is a confirmed renderer inconsistency; whether the tested record contains light strokes or lacks an image still needs on-device confirmation. Do not recover a historical signature from the current profile or fabricate one.

Document fixtures and TypeScript passed. Mike corrected the printer switching report: he had selected the wrong printer; 4387 then printed. No switching regression is proven. The short inter-page gap has no explicit tear-off timer in the current Brother report loop.

## vc46 verified and More alignment

Both S24 and ZFold installed vc46; package version read back successfully. APK SHA-256 EB5689C47DD5BBFC60143F227EECBDBD5E984DADF2FEFEF995471CD83EBCAB98, source 5d35730717779e38dfdd3bd0e1c2416a04c2411d. Mike confirms the signature is visible after reopening the saved Read JSA report. A fresh physical signature print remains unverified.

More popup labels now right-aligned. This and duplicate top-menu removal are committed after vc46 and need the next build.
