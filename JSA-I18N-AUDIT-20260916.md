# WB JSA English/Spanish audit — 2026-09-16

## Source

- Repository: `C:\dev\JSA`
- Branch: `fix/jsa-app-access-handoff-20260913`
- Starting commit: `7d482260376d52db1d8d53066e8778b0267b93cb`

## Result

- Completed an English/Spanish UI audit across routes, shared components, governed access screens, printer flows, open and saved JSA screens, login/logout, location additions, and the app switcher.
- Expanded the Spanish catalog to 604 entries and added placeholder interpolation for values such as names, dates, page counts, and printer names.
- Localized both JSA HTML/report builders, including print previews, saved-record reports, thermal output, generated/uploaded PDFs, headings, checklist labels, timestamps, and location additions.
- Added `tools/test-i18n.cjs` to fail when a literal `t(...)` key has no Spanish entry, when required dynamic UI copy is missing, or when direct visible `<Text>` copy bypasses translation.
- Kept customer-authored company names, well names, assessment wording, contact names, and manually entered content unchanged. Built-in assessment wording continues to translate through the existing catalog.

## Verification

- `node tools/test-i18n.cjs` — passed, 604 Spanish entries.
- `npx tsc --noEmit` — passed.
- `npm run lint` — passed with 0 errors; the repository still reports 76 pre-existing warnings.
- All 18 `tools/test-*.cjs` checks passed.
- `git diff --check` — passed (Git reports only the repository's normal LF-to-CRLF notices on Windows).

## Build state

No APK was built, installed, or launched for this checkpoint, as requested.
